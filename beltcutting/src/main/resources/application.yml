spring:
  profiles:
    active: dev
    group:
      dev:
        - local-dev
        - debug
  jpa:
    open-in-view: false
server:
  error:
    include-message: always
    include-binding-errors: always
springdoc:
  show-actuator: true
management:
  endpoint:
    health:
      show-details: always